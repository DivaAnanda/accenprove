# Database (SQLite - file-based, no configuration needed)
# File will be created automatically: sqlite.db

# JWT Secret (MUST CHANGE in production!)
# Generate secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Email Configuration (Gmail SMTP)
# Setup: Create Gmail App Password (Settings → Security → 2-Step Verification → App passwords)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password

# App URL (for email verification links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ========================================
# SETUP INSTRUCTIONS FOR NEW DEVELOPERS:
# ========================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Update JWT_SECRET with a secure random string (min 32 chars)
#
# 3. Email Setup (for password reset & verification):
#    a. Create/Use Gmail account for this project
#    b. Enable 2-Factor Authentication: https://myaccount.google.com/security
#    c. Generate App Password:
#       - Go to: 2-Step Verification → App passwords
#       - Select: Mail → Other (Custom name: "Accenprove")
#       - Copy the 16-character password (remove spaces)
#    d. Update EMAIL_USER and EMAIL_PASSWORD above
#
# 4. Initialize Database:
#    npm run db:push
#
# 5. (Optional) Seed Demo Data:
#    npm run db:seed
#
# 6. Start Development Server:
#    npm run dev
# ========================================
